// import { createClient } from '@supabase/supabase-js';

// TODO: Configurar conexión a la base de datos independiente de Nina.
// Asegúrate de crear un nuevo proyecto en Supabase para este portal
// y añadir las credenciales en un archivo .env.local

/*
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
*/

export const supabase = null; // Placeholder hasta que se configure la DB
